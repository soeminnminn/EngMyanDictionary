<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/dialog_full_holo_light" >
    
    <RelativeLayout 
        android:id="@+id/frameCreditTitle"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize" >
        
        <TextView
	       android:layout_width="wrap_content"
	       android:layout_height="match_parent"
	       android:layout_marginLeft="@dimen/popup_divider_margin"
	       android:layout_marginStart="@dimen/popup_divider_margin"
	       android:textAppearance="?android:attr/textAppearanceMedium"
	       android:textStyle="bold"
	       android:gravity="center_vertical"
	       android:text="@string/prefs_credit" />
        
        <ImageButton
       		android:id="@+id/closeButton"
	        style="@style/ActionbarButton"
	        android:layout_width="?attr/actionBarSize"
	        android:layout_centerVertical="true"
	        android:layout_marginRight="4dp"
        	android:layout_marginEnd="4dp"
	        android:layout_height="wrap_content"
	        android:layout_alignParentRight="true"
	        android:layout_alignParentEnd="true"
	        android:contentDescription="@string/action_close"
	        android:src="@drawable/ic_close_gray" />
        
    </RelativeLayout>
    
    <View 
        android:layout_width="match_parent"
        android:layout_height="1dip"
        android:layout_marginTop="?attr/actionBarSize"
        android:background="@drawable/divider_background" />
    
    <WebView
        android:id="@+id/webViewCredit"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/frameCreditTitle" />

</RelativeLayout>
